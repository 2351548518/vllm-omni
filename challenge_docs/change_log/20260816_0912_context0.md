# P6-D：`codec_left_context_frames=0` 参数实验

## 状态

待运行。独立 worktree：`/workspace/user_data/vllm-omni-worktrees/p6d-context0`，分支
`exp/p6d-context0`，提交 `ec04185f`。相对默认配置只改变
`connector_of_shared_memory.extra.codec_left_context_frames: 3 → 0`；chunk=25、
Token2Wav steps=10、float16=false，采样和 stage 配置不变。

## 风险与验收

减少左上下文可能造成 chunk 边界不连续、音频时长变化或全双工 barge-in/fence
语义问题。必须先运行 Chapter 10 三档性能，再跑 Seed-TTS WER/SIM（含失败、无
PCM、音频时长）；只有无可测质量回退且性能可重复改善才考虑 full-duplex 验证。

## 结果

已由 tmux watcher `minicpm_opt_queue_after_chunk30` 启动，性能完成、Seed-TTS
随后运行。性能日志名为 `p6d_context0_simplex_20260816_092600.log`，精度日志名为
`p6d_context0_seed_accuracy_20260816_092600.log`；结果不覆盖默认基线。

### Chapter 10 性能结果

三档请求均完成、失败为 0，JSON 位于 `challenge_docs/batch_result/p6d_context0_simplex/`：

| 并发/请求数 | 吞吐 req/s | TTFT ms | E2EL ms | audio TTFP ms | audio RTF | completed/failed |
|---|---:|---:|---:|---:|---:|---:|
| 1/32 | 0.466676 | 316.631 | 2142.404 | 1029.663 | 0.629414 | 32/0 |
| 4/64 | 0.568262 | 475.594 | 6987.442 | 3604.047 | 2.014260 | 64/0 |
| 8/128 | 0.679160 | 477.453 | 11683.507 | 4534.320 | 3.416617 | 128/0 |

相对 AGENT.md A3 基线，吞吐变化约 `-5.78%/-1.09%/-9.49%`，audio RTF 变化约
`+27.57%/+17.23%/+30.21%`；三档均明显退化。

### Seed-TTS 失败结果

Seed-TTS 在约第 133 个请求时，Stage2 `SharedMemoryConnector` 报
`MessagePack data is malformed: trailing characters (byte 1)`，stage-1 EngineCore
退出。结果 JSON
`challenge_docs/batch_result/p6d_context0_accuracy/qwen_omni_acc_seed_tts_20260816-110921.json`
记录 `completed=137`、`failed=1883`、`no_pcm=1`，WER/SIM 仅对少量成功样本计算，
不能作为精度通过；完整日志：
`challenge_docs/run_log/p6d_context0_seed_accuracy_20260816_092600.log`。

context0 拒绝：三档性能退化且减少上下文同样暴露 SHM MessagePack 边界崩溃；正式配置
继续 `codec_left_context_frames=3`。该失败与 chunk30 的相同根因需在 P3 SHM profiling
中优先复现和修复，不能归因成单纯音频质量波动。
