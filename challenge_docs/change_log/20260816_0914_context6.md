# P6-D：`codec_left_context_frames=6` 参数实验

## 状态

待运行。独立 worktree：`/workspace/user_data/vllm-omni-worktrees/p6d-context6`，分支
`exp/p6d-context6`，提交 `c61778fb`。相对默认配置只改变
`connector_of_shared_memory.extra.codec_left_context_frames: 3 → 6`；chunk=25、
Token2Wav steps=10、float16=false，采样和 stage 配置不变。

## 风险与验收

增加左上下文会扩大每个 chunk 的重叠计算，可能增加 TTFP/RTF 和显存，同时必须
检查边界连续性、音频总时长及全双工 turn/fence 顺序。必须先运行 Chapter 10 三档
性能，再跑 Seed-TTS WER/SIM 和失败/无 PCM统计；无可测质量回退且性能可重复改善
后才进入全双工验证。

## 结果

已由 tmux watcher `minicpm_opt_queue_after_context0` 启动，性能在 8/128 阶段触发
服务崩溃，Seed-TTS 会在性能 fixture 收尾后继续执行。性能日志名为
`p6d_context6_simplex_20260816_092700.log`，精度日志名为
`p6d_context6_seed_accuracy_20260816_092700.log`；结果不覆盖默认基线。

### Chapter 10 性能（部分失败）

1/32 和 4/64 请求均完成，结果 JSON 位于
`challenge_docs/batch_result/p6d_context6_simplex/`：

| 并发/请求数 | 吞吐 req/s | TTFT ms | E2EL ms | audio TTFP ms | audio RTF | 状态 |
|---|---:|---:|---:|---:|---:|---|
| 1/32 | 0.507804 | 318.759 | 1968.875 | 1027.950 | 0.426188 | 32/32 |
| 4/64 | 0.578008 | 472.755 | 6834.953 | 3506.222 | 1.505142 | 64/64 |

8/128 在开始约 2 个请求后，Stage2 `SharedMemoryConnector` 报
`MessagePack data is malformed: trailing characters (byte 1)`，stage-1 EngineCore
退出，Chapter 10 pytest 失败。此候选不能以 1/32、4/64 的局部改善通过稳定性门禁。

### Seed-TTS 精度（完成，拒绝）

性能 fixture 收尾后已自动启动 Seed-TTS，仍使用并发 4、2020 条固定数据集和默认
WER/SIM 评估。服务在约 128 条完成后退出，客户端随后记录
`Cannot connect to host 127.0.0.1:55137` 并进行重试；这与性能阶段的
`MessagePack data is malformed: trailing characters (byte 1)` 属于同一稳定性故障链。
结果 JSON 为
`challenge_docs/batch_result/p6d_context6_accuracy/qwen_omni_acc_seed_tts_20260816-114712.json`：
`completed=137/2020`、`failed=1883`、`no_pcm=1`、`WER content_evaluated=136`
（均值约 18.16%）、`SIM=0.849540`；pytest `1 failed, 3 deselected`、退出码 1。
完整日志为 `challenge_docs/run_log/p6d_context6_seed_accuracy_20260816_092700.log`。
候选已因性能 8/128 崩溃、Seed 失败率和无 PCM 拒绝，不进入默认配置。
