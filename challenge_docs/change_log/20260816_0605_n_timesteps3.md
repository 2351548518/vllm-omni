# P6-A：`token2wav_n_timesteps=3` 参数实验

## 状态

`已完成 / 拒绝（精度回退）`。

本轮在 P1 detached-view 代码之外恢复了两个 request-level `.clone()`，只修改 `vllm_omni/deploy/minicpmo_4_5.yaml` 的 connector extra：

```yaml
token2wav_n_timesteps: 3
```

默认值基线为代码中的 `10`。本轮不改变 `codec_chunk_frames`、`codec_left_context_frames`、sampling、max tokens、服务启动参数或全双工配置。

## 测试计划

1. A：使用 AGENT.md 同机 A3 baseline（默认 10；原始性能/精度已归档）。
2. B：当前 3 步配置，Chapter 10 性能 pytest 三组，结果目录 `challenge_docs/batch_result/p6a_n3_simplex/`。
3. B 精度：Chapter 10 Seed-TTS 单项全量 2020 条 WER/SIM，结果目录 `challenge_docs/batch_result/p6a_n3_accuracy/`。
4. 本轮 B 已出现精度回退，已恢复 `10`；不进入 A 回归/保留流程，继续下一个独立变量。

## 性能结果

- tmux：`minicpm_opt_p6a_n3:perf`（已完成，服务自动停止）。
- 原始日志：`challenge_docs/run_log/p6a_n3_simplex_20260816_060321.log`。
- JSON：`challenge_docs/batch_result/p6a_n3_simplex/` 下的三份 `1_32`、`4_64`、`8_128` 结果；三组均完成且 `failed == 0`。

| 并发/请求数 | 吞吐 req/s | TTFT ms | E2EL ms | audio TTFP ms | audio RTF |
|---|---:|---:|---:|---:|---:|
| 1/32 | 0.65553 | 317.77 | 1525.10 | 758.44 | 0.36817 |
| 4/64 | 1.08782 | 461.90 | 3639.50 | 2422.14 | 0.92392 |
| 8/128 | 2.05872 | 518.88 | 3841.54 | 2337.88 | 0.96875 |

相对 AGENT.md A3 baseline，性能初步显著改善，但不能替代精度和 A/B/A 门禁。

相对 AGENT.md A3 baseline 的单次变化：

| 组 | 吞吐 | TTFT | E2EL | audio TTFP | audio RTF |
|---|---:|---:|---:|---:|---:|
| 1/32 | +32.35% | -0.07% | -24.44% | -27.88% | -25.38% |
| 4/64 | +89.36% | -10.61% | -46.93% | -32.75% | -46.29% |
| 8/128 | +174.36% | +0.76% | -63.60% | -45.86% | -63.10% |

## Seed-TTS 精度

- tmux：`minicpm_opt_p6a_n3:seed`。
- 原始日志：`challenge_docs/run_log/p6a_n3_seed_accuracy_20260816_061503.log`。
- 结果目录：`challenge_docs/batch_result/p6a_n3_accuracy/`。
- 全量 2020 条、并发 4、WER/SIM 开启；pytest `1 passed, 3 deselected, 15 warnings`，总耗时 `2335.17s`。

| 指标 | A3 baseline | n_timesteps=3 | 变化 |
|---|---:|---:|---:|
| WER | 1.3875% | 1.4542% | +0.0667pp（回退） |
| WavLM SIM | 0.848603 | 0.846267 | -0.002336（回退） |
| completed / failed | 2020 / 0 | 2020 / 0 | 无变化 |
| no PCM / ASR failed / SIM failed | 0 / 0 / 0 | 0 / 0 / 0 | 无变化 |

性能 JSON 的全量 Seed-TTS 汇总：吞吐 `1.26628 req/s`、mean TTFT `513.78ms`、mean E2EL `3155.50ms`、audio TTFP `1452.54ms`、audio RTF `0.63184`、音频总时长 `10235.44s`。

## 验收

必须同时查看 audio TTFP、audio RTF、E2EL、吞吐、音频时长、PCM/请求失败、WER/SIM；本轮性能改善但 WER/SIM 均回退，已按门禁拒绝 `3` 并恢复 `10`。full-duplex 未运行，因此不能将其作为可保留候选。

## 代码快照

为保留该方案的可复现实验状态，P6-A worktree 已确认干净并固定为：

- worktree：`/workspace/user_data/vllm-omni-worktrees/p6a-n3`
- 分支：`exp/p6a-n3`
- commit：`b65a12e95ef4041f0f4fde71ca49e7da48ea438d`
- annotated tag：`exp/p6a-n3-20260816`
- 快照内容：仅将 `vllm_omni/deploy/minicpmo_4_5.yaml` 的 `token2wav_n_timesteps` 从 `10` 改为 `3`；主分支默认配置仍为 `10`

该 tag 是实验归档快照，不代表候选通过精度门禁。

## 后续合入记录（2026-08-17）

按用户要求，P6-A 已从该 tag 合入 `minicpm-challenge`：

- 合并提交：`8555eeb91b319e053867276c20df9e28d521ebbe`
- 合并内容：仅将默认部署配置的 `token2wav_n_timesteps` 设为 `3`
- 已知限制：Seed-TTS WER/SIM 相对 A3 baseline 有可测回退，且本轮没有 full-duplex 验证；因此该合入是用户明确选择的性能/质量折中，不代表通过 AGENT.md 的无损精度门禁
