# 2026-08-16 04:35 UTC — Code2Wav 状态缓存冗余 clone 优化

## 状态

`候选结果完成 / 单次性能与 Seed-TTS 通过；A/B/A 与 full-duplex 仍待补齐`。

## 基线与环境

- 基线提交：`3091081f`（A3 baseline 与优化思路已推送到 `origin/minicpm-challenge`）。
- 设备：Ascend 910C / A3，`npu-smi 25.5.0`，HBM 65536 MB；当前可见 NPU 为 4 号设备实例（物理 ID 0/1）。
- 模型：`/workspace/shared_assets/models/OpenBMB/MiniCPM-o-4_5`。
- 普通部署：`vllm_omni/deploy/minicpmo_4_5.yaml`。
- 参考性能基线：`challenge_docs/run_log/baseline_simplex_performance_20260815_111642.log` 及 `challenge_docs/batch_result/origin/simplex_performance/`。
- 参考精度基线：`challenge_docs/run_log/baseline_accuracy_20260815_113536.log`；Seed-TTS WER 1.39%、WavLM SIM 0.8486。

## 修改

文件：`vllm_omni/model_executor/models/minicpmo_4_5/batched_token2wav.py`

1. `_split_flow_cache` 对刚生成且后续不再写入的 conformer cache 使用 detached view，移除逐 request `clone()`。
2. `decode_batch` 对刚由 HiFT 返回、下一轮会通过 `torch.cat` 重新组 batch 的 mel/source/speech cache 使用 detached row view，移除逐 request、逐 chunk 的 `clone()`。

`_stack_flow_cache` 和 HiFT 输入仍然会构造新 batch tensor；request cache 仍由各自 row view 保持引用，未改变 CFM/HiFT 数值、CFG 行顺序、cache epoch 或 full-duplex metadata。

## 局部验证

```text
python -m compileall -q vllm_omni/model_executor/models/minicpmo_4_5/batched_token2wav.py
pytest ... test_code2wav_batching.py ...
```

结果：去掉依赖 `mocker` 的测试后 `53 passed, 1 deselected, 15 warnings`；进程退出码为 134，并出现已知 `corrupted size vs. prev_size` 清理崩溃。完整首次运行还因环境缺少 `pytest-mock` 的 `mocker` fixture 报 `1 error`，不是补丁断言失败。该环境问题和原始 stdout/stderr 需在最终结果中保留。

## 待运行命令

所有长任务使用 tmux 会话 `minicpm_opt_p1`，日志分别保存到 `challenge_docs/run_log/`；性能命令沿用 `challenge_docs/minicpm_challenge_recode.md` 第十章固定的三组 A3 simplex 配置，精度只运行 Seed-TTS WER/SIM：

```bash
BENCHMARK_DIR=/workspace/user_data/vllm-omni/challenge_docs/batch_result/p1_clone_view_simplex \
  python -m pytest -s -v tests/dfx/perf/scripts/run_benchmark.py \
  --test-config-file /workspace/user_data/vllm-omni/challenge_docs/test_yaml/test_minicpmo_4_5.json \
  -k test_minicpmo_4_5_challenge
```

```bash
ACC_BENCH_RESULT_DIR=/workspace/user_data/vllm-omni/challenge_docs/batch_result/p1_clone_view_accuracy \
  ASCEND_RT_VISIBLE_DEVICES=0 \
  VLLM_TEST_MINICPMO_4_5_MODEL=/workspace/shared_assets/models/OpenBMB/MiniCPM-o-4_5 \
  VLLM_SEED_TTS_DATASET_PATH=/workspace/user_data/datasets/seed-tts-eval/seedtts_testset \
  ACC_BENCH_SEED_TTS_MAX_CONCURRENCY=4 \
  SEED_TTS_WER_EVAL=1 SEED_TTS_SIM_EVAL=1 \
  pytest -s -v -rs tests/e2e/accuracy/minicpmo_4_5/test_minicpmo_4_5.py \
  -m 'full_model' -k 'seed_tts_wer_bench and not duplex' --run-level full_model
```

## 验收与回退

- 三组 `(1,32)/(4,64)/(8,128)` 的 audio TTFP、audio RTF、TTFT、E2EL/吞吐需在同一 A3 环境重复测量并有可解释改善；失败率、音频时长和 PCM 连续性不得退化。
- Seed-TTS WER/SIM、无 PCM、ASR/请求失败不得相对基线回退；若有任何状态串扰、音频时长变化或 full-duplex 增量异常，立即回退这一补丁。
- 本轮只验证普通流式性能并运行 Seed-TTS 单项；保留进入下一轮前必须补做的 full-duplex 门禁，不将 simplex 结果当作 full-duplex 通过。

## 结果

### 第十章命令核对

- 第十章的性能命令和精度命令都是 pytest 驱动的自动化入口；不需要另起一个常驻 `vllm serve`。性能 runner 会自动在临时端口启动 OmniServer，精度测试的 `omni_server` fixture 会自动启动服务，测试函数随后再调用 `vllm bench serve`。
- 本轮实际性能日志包含 `Starting OmniServer` → `Server ready` → 三组 benchmark → `OmniServer stopped`；未使用外部手工服务参与统计。此前为排查端口曾短暂启动过一个手工服务，但在正式性能命令前已停止，不在结果链路中。
- 本轮 Seed-TTS 日志包含 `Launching OmniServer`、`Server ready on 127.0.0.1:49529`，随后打印内部 `$ ... vllm bench serve ... --num-prompts 2020 --max-concurrency 4 ... --seed-tts-wer-eval --seed-tts-file-ref-audio`。这与第十章单项过滤命令的自动起服务链路一致；只将结果目录改为 `p1_clone_view_accuracy` 以避免覆盖 baseline，并省略了被 `-k` 排除的 Daily/Video 数据集环境变量。

### 性能（已完成）

- tmux 会话：`minicpm_opt_p1:perf`（性能完成后会话自然退出；未影响用户已有的 `0` 会话）。
- 原始日志：`challenge_docs/run_log/p1_clone_view_simplex_20260816_044425.log`。
- 原始 JSON：`challenge_docs/batch_result/p1_clone_view_simplex/` 下的 `1_32`、`4_64`、`8_128` 三份结果。
- pytest：`1 passed, 1 deselected, 15 warnings`，总耗时 `756.74s`；三组均 `completed == num_prompts` 且 `failed == 0`。

| 并发/请求数 | 吞吐 req/s | TTFT ms | E2EL ms | audio TTFP ms | audio RTF |
|---|---:|---:|---:|---:|---:|
| 1/32 | 0.51438 | 316.51 | 1943.76 | 1029.14 | 0.47568 |
| 4/64 | 0.60797 | 465.08 | 6491.15 | 3341.36 | 1.62408 |
| 8/128 | 0.74332 | 459.25 | 10669.58 | 4182.06 | 2.64771 |

相对 `AGENT.md` 中同一 A3 设备重测基线，`1/32` 和 `4/64` 的吞吐/E2EL/audio TTFP/audio RTF 均改善；`8/128` 吞吐和 audio RTF 分别约回退 `0.94%`、`0.88%`，E2EL 约回退 `1.09%`，属于单次噪声范围但尚未达到 A/B/A 验收条件。压测 JSON 内嵌的 `baseline.A3` 与 AGENT.md 当前基线不是同一轮数值，不能混用作验收依据；完整对照见 `challenge_docs/result/20260816_0500_p1_clone_view_summary.md`。

### Seed-TTS 精度（已完成）

- tmux 会话：`minicpm_opt_p1:seed`。
- 原始日志：`challenge_docs/run_log/p1_clone_view_seed_accuracy_20260816_045807.log`。
- 运行命令使用 Chapter 10 的 `full_model` + `seed_tts_wer_bench and not duplex` 过滤，`SEED_TTS_WER_EVAL=1`、`SEED_TTS_SIM_EVAL=1`、并发 `4`，全量 `2020` 条中文 Seed-TTS；结果目录为 `challenge_docs/batch_result/p1_clone_view_accuracy/`。
- pytest：`1 passed, 3 deselected, 15 warnings`，总耗时 `3772.74s`；服务已自动停止，NPU 已释放。
- 原始 JSON：`challenge_docs/batch_result/p1_clone_view_accuracy/qwen_omni_acc_seed_tts_20260816-050235.json`。
- `2020/2020` 完成，HTTP 失败 `0`、无 PCM `0`、ASR/WER 失败 `0`、SIM 失败 `0`；WER `1.3606%`，WavLM SIM `0.848419`。
- 相对 AGENT.md A3 精度基线 WER `1.3875%`、SIM `0.848603`：WER 改善约 `0.027pp`，SIM 下降约 `0.00018`，目前没有可测质量回退迹象。

### 当前判定

本轮补丁单次性能与 Seed-TTS 结果均已归档，暂标记为“候选通过、未最终验收”：性能仍需按 AGENT.md 补齐 A/B/A，且必须补做 full-duplex 门禁。若后续重复性能或 full-duplex/精度有回退，恢复本文件所述两个 `.clone()`。

### 后续串行复测排队

为补齐 A/B/A，已安排 tmux watcher `minicpm_opt_queue_after_context0` 在当前
参数队列（chunk20/chunk30/context0/context6）全部结束后依次执行：默认配置 A1、
`exp/p1-clone-view` B、默认配置 A2 的第十章三档性能，然后在 B 上运行 Seed-TTS。
对应结果目录前缀为 `p1_ab_a1_simplex`、`p1_ab_b_simplex`、`p1_ab_a2_simplex`
和 `p1_ab_accuracy`；主分支不切换代码，A/B/A 每一步均保留独立原始日志。

### A/B/A 复测现场（2026-08-16 12:xx UTC）

- A1 默认配置已经完成 `(1,32)`、`(4,64)`，分别为 `32/32`、`64/64`；在
  `(8,128)` 运行到 `25/128` 后，默认配置的 Stage-1 EngineCore 先退出。日志中的根因是
  `SharedMemoryConnector shm get failed ... MessagePack data is malformed: trailing characters
  (byte 1)`，随后请求返回 HTTP 500。因此这次 A1 不是性能断言或 P1 代码导致的数值不达标，而是
  当前基线在高并发下的 SHM framing 稳定性失败；pytest 按设计以
  `assert completed == num_prompts` 报 `1 failed, 1 deselected`。
- B（P1 detached-view）也在 `(8,128)` 仅完成 `25/128`，同样出现上述 SHM 错误并导致服务拒绝连接。
  `(1,32)` 吞吐 `0.511257 req/s`、`(4,64)` `0.592717 req/s`；这组 B 不能作为 P1 的
  通过证据，因为 A1 已失效且 B 自身有请求失败。
- A2 默认配置和随后 Seed-TTS 仍在队列中；在 A2 结束前不作 A/B/A 判定。后续 P3 的长度 framing
  修复专门针对这一条错误，必须在独立 worktree 中重新跑完整性能、Seed-TTS 和 full-duplex 门禁。

补充：A2 默认配置已结束，`(1,32)`/`(4,64)` 分别完成 `32/32`、`64/64`，吞吐为
`0.517761`、`0.590626 req/s`；`(8,128)` 完成 `26/128`，`102` 个请求失败，随后同样
出现 `MessagePack data is malformed: trailing characters (byte 1)`。所以 A1、B、A2 三段
均在高并发边界失效，P1 的严格 A/B/A 性能结论无效，不能据此接受或回退 clone-view。
队列仍会执行 B 版本 Seed-TTS，以保留精度现场；真正的对照应在 P3 framing 或其他稳定性修复
之后重新建立。

截至 12:33 UTC，B Seed-TTS 已自动启动 Chapter 10 内部 `vllm bench serve`，进度约
`18/2020`，当前观测请求均返回 200；结果 JSON 尚未写出，测试仍在运行。

### B Seed-TTS 最终结果

该轮最终在 `20260816-123919` 生成 JSON：仅 `137/2020` 完成，`1883` 请求失败、`1`
条无 PCM；在第 `133` 条附近，Stage-2 的 `SharedMemoryConnector` 再次报
`MessagePack data is malformed: trailing characters (byte 1)`，Stage-1 EngineCore
退出，后续请求返回 HTTP 500。WER 仅对 136 条成功音频计算，均值 `2.6308%`，SIM
`0.846211`；pytest 因 WER 超过 2% 且请求失败而失败。该结果进一步证明 P1 不能接受，
并给 P3/P3B 的 SHM 修复提供了稳定复现点。原始 JSON：
`challenge_docs/batch_result/p1_ab_accuracy/qwen_omni_acc_seed_tts_20260816-123030.json`。
