# P6-F：Stage 2 `gpu_memory_utilization=0.30` 容量实验

## 候选

- worktree：`/workspace/user_data/vllm-omni-worktrees/p6f-stage2-mem`
- 分支/提交：`exp/p6f-stage2-mem` / `4971f783`
- 唯一变量：Stage 2 `gpu_memory_utilization: 0.18 → 0.30`；`max_num_seqs`、token
  cap、Code2Wav 参数和其他 stages 不变。
- 动机：记录的 Stage 0/1 KV/HBM headroom 与 Stage 2 Code2Wav activation 可能造成
  admission/排队；该实验只验证容量配置是否改善高并发，不把更高占用当作必然加速。
  风险为 HBM 预留过量、三阶段启动冲突、OOM/KV preemption 和 SHM 稳定性。

## 测试队列

等待 P2 graph A/B/A+Seed 退出后执行默认 A1 → P6-F B → 默认 A2 → P6-F Seed-TTS。
记录 startup HBM/KV、OOM/preemption、Chapter10 三档指标、失败/PCM 统计和 Seed
WER/SIM；任何容量或质量门禁失败都拒绝且保留 worktree。

## 结果

待长测完成后填写；默认 Stage 2 仍为 0.18。
