# P6-B：`token2wav_float16` 参数实验

## 状态

`A 性能完成，A Seed-TTS 运行中`。

本轮先恢复了 `token2wav_n_timesteps: 10`，只准备测试 connector extra 中的
`token2wav_float16: false → true → false`。不改变采样、chunk/context、stage
capacity、全双工配置或代码路径。

## 测试计划

1. A：当前默认 `token2wav_float16` 未设置（代码默认 `false`），Chapter 10
   性能三组，结果目录 `challenge_docs/batch_result/p6b_base_simplex/`。
2. A Seed-TTS：全量 2020 条 WER/SIM，独立 tmux 任务。
3. B：仅增加 `token2wav_float16: true`，重复同样性能和 Seed-TTS 测试。
4. 若 B 精度无回退，再恢复 `false` 做 A 回归；否则立即恢复并记录拒绝。

## 运行记录

- 性能 tmux：`minicpm_opt_p6b_base_perf:perf`（已完成，pytest `1 passed`）。
- 性能原始日志：`challenge_docs/run_log/p6b_base_simplex_20260816_065518.log`。
- 性能 JSON：`challenge_docs/batch_result/p6b_base_simplex/`。
- A Seed-TTS tmux：`minicpm_opt_p6b_base_seed:seed`（全量请求进行中）。
- A Seed-TTS 原始日志：`challenge_docs/run_log/p6b_base_seed_accuracy_20260816_071400.log`。
- A Seed-TTS 结果目录：`challenge_docs/batch_result/p6b_base_accuracy/`。
- 服务必须由 Chapter 10 的 pytest fixture 自动启动和停止；不复用手工服务。

### A 性能结果

| 并发/请求数 | 吞吐 req/s | TTFT ms | E2EL ms | audio TTFP ms | audio RTF | completed/failed |
|---|---:|---:|---:|---:|---:|---:|
| 1/32 | 0.510417 | 325.266 | 1958.817 | 1034.930 | 0.481502 | 32/0 |
| 4/64 | 0.571796 | 476.358 | 6915.007 | 3508.328 | 1.708354 | 64/0 |
| 8/128 | 0.706360 | 464.688 | 11218.247 | 4387.393 | 2.779650 | 128/0 |

A Seed-TTS 已完成：pytest `1 passed, 3 deselected, 15 warnings`，总耗时 `3836.66s`。

| 指标 | A 默认 float16=false |
|---|---:|
| WER | 1.3785% |
| WavLM SIM | 0.848655 |
| completed / failed | 2020 / 0 |
| no PCM / ASR failed / SIM failed | 0 / 0 / 0 |
| mean TTFT / E2EL | 547.636 / 6054.872 ms |
| mean audio TTFP / RTF | 2322.919 ms / 1.212400 |
| total audio duration | 10248.92 s |

结果 JSON：`challenge_docs/batch_result/p6b_base_accuracy/qwen_omni_acc_seed_tts_20260816-071813.json`。

## 验收

同时比较三组吞吐、TTFT、audio TTFP、E2EL、audio RTF、音频时长、失败/PCM
统计，以及 Seed-TTS WER/SIM。任何精度、PCM 或全双工边界回退都不保留
float16 配置。
