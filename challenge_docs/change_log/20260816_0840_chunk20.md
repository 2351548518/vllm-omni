# P6-C：`codec_chunk_frames=20` 参数实验

## 状态

`性能完成，Seed-TTS 运行中`。

独立 worktree：`/workspace/user_data/vllm-omni-worktrees/p6c-chunk20`，分支
`exp/p6c-chunk20`，提交 `0a61a41d`。本轮基于默认 `token2wav_n_timesteps=10`、
`codec_left_context_frames=3`、float16=false，只把 connector extra 的
`codec_chunk_frames` 从 25 改为 20；保留 CANN 原始 `PYTHONPATH`，不改变采样、
stage capacity、全双工协议或服务参数。

## 测试计划

1. Chapter 10 性能三组，结果 `challenge_docs/batch_result/p6c_chunk20_simplex/`。
2. Seed-TTS 全量 WER/SIM，结果 `challenge_docs/batch_result/p6c_chunk20_accuracy/`。
3. 记录 chunk 次数、音频时长、PCM/请求失败；若 20 无质量回退，再建立独立
   `codec_chunk_frames=30` worktree 测试。

## 运行记录

- worktree 代码必须通过 `PYTHONPATH=<worktree>:$PYTHONPATH` 启动，以保留 Ascend
  CANN `acl.so` 路径。
- 性能/精度原始日志和 JSON 将使用 `p6c_chunk20_*` 前缀，不覆盖默认 A 对照。

### 性能结果

性能 tmux：`minicpm_opt_p6c_chunk20_perf:perf`，pytest `1 passed, 1 deselected`，
总耗时 `871.01s`。原始日志：`challenge_docs/run_log/p6c_chunk20_simplex_20260816_084500.log`。

| 并发/请求数 | 吞吐 req/s | TTFT ms | E2EL ms | audio TTFP ms | audio RTF | completed/failed |
|---|---:|---:|---:|---:|---:|---:|
| 1/32 | 0.493937 | 318.651 | 2024.171 | 963.649 | 0.491846 | 32/0 |
| 4/64 | 0.540157 | 481.867 | 7338.573 | 3290.792 | 1.804426 | 64/0 |
| 8/128 | 0.629952 | 478.026 | 12580.441 | 4356.664 | 3.088987 | 128/0 |

相对 P6-B A 默认配置（0.510417/0.571796/0.706360 req/s；audio RTF
0.481502/1.708354/2.779650），chunk20 的吞吐和 E2EL/RTF 均退化；Seed-TTS
仍需完成以检查质量和边界指标。

## 验收

除 TTFT、audio TTFP、E2EL、吞吐、audio RTF 外，重点检查 chunk boundary 的 PCM
连续性、音频总时长和 full-duplex 的 segment/turn/fence 顺序。任何边界伪影、PCM
缺失、请求失败或 WER/SIM 回退都拒绝该值。
